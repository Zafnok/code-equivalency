# IOperation lowering coverage

Maintained by the Frontend.CSharp tickets. One row per `OperationKind`. Status:
`lowered` (with test name), `opaque` (falls back to `IrOpaque`), `n/a` (never appears
in a CFG body). Every kind found in `samples/` must have a row
(`SampleLoweringTests.EveryOperationKindInTheSamplesHasACoverageRow`).

| OperationKind | Status | Test | Ticket |
|---|---|---|---|
| Argument | lowered: inside `Invocation`: its value, passed in parameter order | IrLowererTests.NamedArgumentsArePassedInParameterOrder | M2-003 |
| ArrayCreation | lowered: one `int` dimension: a negative length branches to `IrThrow("System.OverflowException")`, then a fresh reference read from `new.<Sort>` at the body's creation count of that sort, `length.<Sort>` written at it (and `Ref`), `array.<Sort>` written at it with a constant map of the element type's default, and an initialiser's values stored at indices 0, 1, ... in order; several dimensions, a jagged creation, another length type, or an element type with no constant default (a struct) are opaque with reason `ArrayCreation` | IrLowererSnapshotTests.ArrayCreation, .ArrayInitializer; IrLowererTests.AnArrayCreationIsNotOpaque, .ANegativeLengthThrowsOverflow, .ANewArrayHoldsItsDefaultsOrItsInitialiser, .AnUnsupportedArrayCreationIsOpaque; TypeMapperTests.DefaultIsTheConstantOfADefault | P2-001 |
| ArrayElementReference | lowered: one `array.<Sort>` map per array sort, keyed by the array reference and then by the bv32 index (a read is two `mapread`s; a write reads the slice, writes the element and writes the slice back), with an unsigned bound against `length.<Sort>` read at the same reference and a branch to `IrThrow("System.IndexOutOfRangeException")`; several dimensions, a long index or an array that is neither a plain variable nor an array creation (P2-001) are opaque with reason `ArrayElementReference` | IrLowererSnapshotTests.ArrayElements; IrLowererTests.AnArrayElementIsAMapKeyedByTheArrayThenTheIndex, .Aliased_ArrayParameters_AreNotDisjoint, .AnIndexOutsideTheArrayThrowsIndexOutOfRange; LoweringOracleTests.LoweredIrAgreesWithCompiledCSharp | M2-004, P1-006 |
| Await | opaque: whole body, reason `async`: an async method's state machine is not modelled (ticket M4-006) | IrLowererTests.WholeBodyIsOneOpaque, .AsyncTaskOfTValidates, .AsyncMethodNeverReportsAwaitOrMissingReturn | M3-026 |
| Binary | lowered: integral and bool operands; overflow, divide-by-zero and `MinValue / -1` edges; `x == null` and `x != null` read the shadow; `string + string` is a call to `System.String::Concat`; `float`, `double` and `decimal` operands are an `IrPure` of `f32.<op>`, `f64.<op>` or `dec.<op>` (M4-002), a `decimal` exception flag branching to a throw of its exact type (`System.OverflowException`, `System.DivideByZeroException`); a user-defined operator, `string ==` and `!=` included, is an `IrPure` of `op:<identity>` whose flag throws `System.Exception`; a lifted operator, and anything else, opaque with reason `Binary` | IrLowererSnapshotTests.StraightLineArithmetic, .Division, .Shifts, .DecimalArithmetic, .FloatingPointComparisonAndConversion, .UserDefinedOperatorAndStringEquality; IrLowererTests.AComparisonWithNullReadsTheShadow; PureLoweringTests.DecimalAdditionIsASharedPureFunction, .DecimalDivisionThrowsDivideByZeroExactly, .EveryDecimalOperatorIsItsCataloguedFunction, .DoubleComparisonIsPure, .FloatingPointArithmeticNeverThrows, .StringEqualityIsTheUserDefinedOperator, .UserDefinedOperatorsAndConversionsAreTheirOpFunctions, .LiftedOrWidenedOperatorsStayOpaque; LoweringOracleTests.LoweredIrAgreesWithCompiledCSharp; PureOperatorTests.OverflowCatchOnDecimalIsNotEquivalent | M2-004, M4-002 |
| Block | n/a: the CFG flattens blocks; an arrow-bodied accessor body is one whole-body opaque with reason `Block` | IrLowererTests.WholeBodyIsOneOpaque | M2-003 |
| CaughtException | n/a: only a `when` filter's CFG reads it, and a filtered `catch` is a whole-body opaque | IrLowererTests.WholeBodyIsOneOpaque | M2-004 |
| CompoundAssignment | lowered: integral local or parameter target: read, promote, operate (same exception edges as the binary operator), narrow back, write; a property target with a getter and a non-init setter: the receiver and index arguments once, the getter call, the operation, the setter call (M3-010); the right operand is evaluated after the target is read; any other target opaque with the target's kind, a non-integral one with reason `CompoundAssignment` | IrLowererSnapshotTests.CompoundAssignment, .CompoundAssignmentToAProperty; IrLowererTests.CompoundAssignmentReadsOperatesAndWrites, .CompoundAssignmentToAnUnsupportedTargetIsOpaque, .CompoundAssignmentToAPropertyGetsOperatesAndSets | M2-004, M3-010 |
| Conditional | n/a: `if` and `?:` become CFG branches, lowered as `br` and phis | IrLowererSnapshotTests.IfElse, .NestedIf | M2-003 |
| ConstantPattern | lowered: inside `IsPattern` | IrLowererTests.AConstantPatternOutsideASwitchIsAnEquality | M2-004 |
| ConstructorBodyOperation | lowered: an instance constructor's CFG, its base or `this` initializer an `Invocation` on the `this` input first; one that does not chain to `this(...)` in a type declaring an instance field or property initializer is one whole-body opaque with reason `field-initializer`, since the initializers are not in its operation tree, its span the first such initializer (M3-025); a static constructor, and a primary constructor with base arguments, stay whole-body opaque with reason `ConstructorBodyOperation` | IrLowererTests.ConstructorCallsItsBaseInitializerFirst, .ConstructorChainingToThisIgnoresFieldInitializers, .ConstructorInATypeWithFieldInitializersIsOpaque, .WholeBodyOpaqueSpanIsTheFirstOmittedInitializer, .APrimaryConstructorWithBaseArgumentsIsOneOpaque, .WholeBodyIsOneOpaque | M2-003, M4-001, M3-025 |
| Conversion | lowered: integral to integral (zext/sext/trunc, checked narrowing throws) and any conversion Roslyn folded to a constant; an implicit reference or boxing conversion between different IR types is `mapread` of the `In` input `cast.<From>.<To>` at the operand, with no trace event, and its result's nullness is read from `null.<To>` (M3-010); an identity conversion, such as the one the CFG wraps around a `foreach` collection, is its operand (M4-001); a numeric conversion to or from `float`, `double` or `decimal` is an `IrPure` of `conv.<from>.<to>`, with a `System.OverflowException` flag from floating point to `decimal`, from `decimal` to an integral type, and from floating point to an integral type when checked, the last runtime-sensitive (M4-002); a user-defined conversion whose operand and result are its method's own types is an `IrPure` of `op:<identity>` (M4-002); anything else, a nullable conversion included, opaque with reason `Conversion` | IrLowererSnapshotTests.Conversions, .CheckedConversion, .BoxingAnInt, .FloatingPointComparisonAndConversion; IrLowererTests.UpcastIsAReadOfTheCastMap, .BoxingIsAReadOfTheCastMap, .CastMapAddsNoTraceEvent, .UnsupportedConstructIsOpaqueWithItsName; PureLoweringTests.ConversionsToAndFromFloatingPointAndDecimalAreCatalogued, .AFloatToIntConversionIsMarkedRuntimeSensitive, .X87LegacyFloatIsSideSpecific, .UserDefinedOperatorsAndConversionsAreTheirOpFunctions; LoweringOracleTests.LoweredIrAgreesWithCompiledCSharp | M2-004, M3-010, M4-002 |
| Decrement | lowered: as `Increment` | IrLowererSnapshotTests.IncrementAndDecrement | M2-004 |
| DefaultValue | lowered: a closed reference type's or a `class`-constrained type parameter's `default` is Roslyn's own constant `null`, so it takes the existing `ConstantValue` literal path (element 0 of its sort) with the `null.<Sort>` shadow set, no dedicated lowering arm needed; an unconstrained or `struct`-constrained type parameter's `default`, and a struct type's, are not a compile-time constant and stay opaque with reason `DefaultValue` (P2-003) | IrLowererSnapshotTests.DefaultValueOfATypeParameter; IrLowererTests.DefaultOfAClassConstrainedTypeParameterIsTheNullElement, .UnsupportedConstructIsOpaqueWithItsName | P2-003 |
| DiscardPattern | lowered: inside `IsPattern`: `true` | IrLowererSnapshotTests.SwitchExpression | M2-004 |
| ExpressionStatement | lowered: its operation, value discarded | IrLowererSnapshotTests.OpaqueCall | M2-003 |
| FieldReference | lowered: one `field.<Type>.<Field>` map per field, read and written at the receiver, or at element 0 of the declaring type's sort when the field is static; a reference receiver is null-checked first | IrLowererSnapshotTests.InstanceFieldAndThrow, .StaticFieldWrite; IrLowererTests.AnInstanceFieldIsAMapKeyedByTheReceiver, .AStaticFieldIsTheSameMapKeyedByItsTypeToken | M2-004 |
| FlowCapture | lowered: a variable; a captured local or parameter is also an lvalue | IrLowererSnapshotTests.ConditionalExpression; IrLowererTests.CapturedLocalIsAssignedThroughTheCapture | M2-003 |
| FlowCaptureReference | lowered: a read of the capture variable, or the captured lvalue as an assignment target | IrLowererSnapshotTests.ConditionalExpression | M2-003 |
| ForEachLoop | lowered: as the CFG desugars it: `GetEnumerator`, `MoveNext` and `Current` calls (a struct enumerator is a value; calls are keyed by trace position, ADR 0018) in a loop, and a `try`/`finally` that null-checks a reference enumerator (`IsNull`) and calls `Dispose`, through the `cast.<From>.<To>` map when the CFG converts it; an array's enumerator is the non-generic one, so its `Current` unboxing and its `as IDisposable` are opaque with reason `Conversion` (ticket P1-004) | IrLowererSnapshotTests.ForEachOverList, .ForEachOverIEnumerableOfInt, .ForEachWithBreak; IrLowererTests.ForEachOverAnArrayIsOpaqueOnlyAtItsConversions; LoweringOracleTests.LoweredIrAgreesWithCompiledCSharp | M2-004, M4-001 |
| Increment | lowered: as `CompoundAssignment` with a promoted `1`, on a local, parameter or property target; postfix yields the value read | IrLowererSnapshotTests.IncrementAndDecrement; IrLowererTests.IncrementAndDecrementYieldTheOldValueOnlyWhenPostfix, .CompoundAssignmentToAPropertyGetsOperatesAndSets | M2-004, M3-010 |
| InstanceReference | lowered: the containing instance of a reference type is the `this` input, of the containing type even where the reference is typed as the base (`base(...)`, `base.M()`, M4-001); every other reference kind is opaque with reason `InstanceReference` | IrLowererTests.TheReceiverOfAnInstanceMethodIsTheThisInput, .ABaseCallPassesThisOfTheContainingType, .AStructsThisIsOpaque | M2-004 |
| Invalid | opaque: a method holding a compiler error, an `Invalid` operation or an error-type operation is one whole-body opaque per cause, reason `unbound`, verdict `Unknown(Unbound)` (ADR 0029); only the body overload the frontend does not use still lowers it to reason `Invalid` | IrLowererTests.AnUnboundMethodLowersToOneUnboundOpaque, .FallingOffANonVoidMethodIsMissingReturn | M2-003, M3-024 |
| Invocation | lowered: `IrCall` plus a threw edge to `System.Exception`; a reference receiver is null-checked first; `ref`/`out` argument: reason `ref-argument`; on the legacy side, a call to an `api-equivalences.json` member entry whose adapter addresses its arguments is an `IrCall` to the modern member, keeping the legacy call's guards | IrLowererSnapshotTests.OpaqueCall, .NullChecks; IrLowererTests.DereferencingAPossiblyNullReceiverThrowsNullReferenceException; ApiEquivalenceLoweringTests | M2-004, M3-009 |
| IsNull | lowered: the CFG's null test of a reference-typed operand (a `using` resource, a `foreach` enumerator, `?.` and `??`) reads its nullness as `x == null` does; any other operand is opaque with reason `IsNull` (ticket P2-008) | IrLowererSnapshotTests.UsingStatement, .ForEachOverIEnumerableOfInt; IrLowererTests.UnsupportedConstructIsOpaqueWithItsName | M4-001 |
| IsPattern | lowered: a constant pattern of the scrutinee's own bitvector or Bool type is an equality, a discard is `true`; every other pattern is opaque with reason `switch-pattern` | IrLowererTests.AConstantPatternOutsideASwitchIsAnEquality, .APatternBeyondAConstantIsOpaque | M2-004 |
| Literal | lowered: a bitvector or Bool constant by value; a constant of an uninterpreted sort (string, floating point, `null`) as a designated element of that sort, `null` being element 0 | IrLowererTests.UnsignedAndCharConstantsKeepTheirBits, .ConstantsOfAnUninterpretedSortAreDesignatedElements | M2-004 |
| LocalReference | lowered: SSA variable | IrLowererSnapshotTests.StraightLineArithmetic | M2-003 |
| Lock | opaque: whole body, reason `lock`: its desugaring passes `ref` to `Monitor.Enter` (ticket M4-003); the opaque's span is the first `lock` statement, not the body (ADR 0029 decision 3) | IrLowererSnapshotTests.EntirelyOpaque; IrLowererTests.WholeBodyIsOneOpaque, .WholeBodyOpaqueSpanIsTheConstructNotTheBody | M2-004, M3-025 |
| Loop | lowered: the CFG has no loop constructs, only back edges, which the SSA builder handles; a `foreach` is desugared into one (see `ForEachLoop`) | IrLowererSnapshotTests.WhileLoop, .ForLoop, .DoWhileLoop; IrLowererTests.LoopsLowerWithoutOpaqueNodes | M2-004 |
| MethodBodyOperation | lowered: the root: its CFG is lowered | IrLowererSnapshotTests.* | M2-003 |
| ObjectCreation | lowered: an `IrCall` to the constructor yielding the new object as a `Sort`, plus a threw edge; a `ref`/`out` argument: reason `ref-argument` | IrLowererTests.ObjectCreationIsACallToTheConstructor, .UnsupportedConstructIsOpaqueWithItsName | M2-004 |
| ParameterReference | lowered: SSA variable; a primary-constructor parameter is opaque with reason `ParameterReference` | IrLowererSnapshotTests.RefAndOutParameters; IrLowererTests.PrimaryConstructorParameterIsOpaque | M2-003 |
| PropertyReference | lowered: `Length` on an array variable is `length.<Sort>` read at its reference (P1-006); any other read is a call to the getter, shaped as an `Invocation` of it (receiver null check, receiver then index arguments, `threw` edge); a write (simple, compound, `++`/`--`) calls the setter with the receiver, the index arguments and the value, and a property the CFG captures as an assignment target has its accessors called at the assignment (M3-010); a property with no accessor for the access (a ref-returning one written, an init-only setter) is opaque with reason `PropertyReference` | IrLowererSnapshotTests.ArrayElements, .InstancePropertyRead, .StaticPropertyWrite, .IndexerRead, .CompoundAssignmentToAProperty; IrLowererTests.TheBoundsCheckAndLengthReadTheLengthMapAtTheArray, .PropertyReadIsACallToTheGetter, .PropertyWriteIsACallToTheSetter, .IndexerReadPassesTheIndex, .APropertyCapturedAsAnAssignmentTargetIsWrittenByItsAccessors, .InitOnlySetterOutsideInitializerIsOpaque; LoweringOracleTests.LoweredIrAgreesWithCompiledCSharp | M2-004, M3-010 |
| Return | lowered: a CFG Return branch becomes `ret` | IrLowererSnapshotTests.VoidEarlyReturn | M2-003 |
| SimpleAssignment | lowered: to a local, parameter, captured lvalue, field or array element; other targets opaque with the target's kind; ref assignment opaque with reason `SimpleAssignment` | IrLowererSnapshotTests.StraightLineArithmetic, .StaticFieldWrite; IrLowererTests.UnsupportedConstructIsOpaqueWithItsName | M2-004 |
| Switch | n/a: the CFG turns a `switch` statement into a chain of equality branches on one captured scrutinee, which `SwitchChains` folds back into `IrSwitch` | IrLowererSnapshotTests.SwitchStatement; IrLowererTests.ASwitchOnAnIntegralScrutineeLowersToOneSwitchTerminator | M2-004 |
| SwitchExpression | n/a: the CFG turns it into a chain of `IsPattern` branches, folded as `Switch` is | IrLowererSnapshotTests.SwitchExpression | M2-004 |
| Throw | lowered: `throw new T(...)` records the constructor call then `IrThrow("T")` on T's static type; throwing anything else is opaque with reason `Throw`, and `throw;` with reason `rethrow` | IrLowererSnapshotTests.ThrowOfANewObject; IrLowererTests.ThrowOfANewObjectRecordsTheConstructorCallAndThrowsItsStaticType, .RethrowIsOpaque | M2-004 |
| Try | lowered: the body's blocks, with every exception edge routed to the first `catch` whose type the thrown type converts to implicitly, and the `finally` region's blocks copied onto every path that leaves the `try` (normal, return and throw); a bare `catch` or a `when` filter makes the whole body opaque with reason `catch-filter`, its span the first such `catch` clause (ADR 0029 decision 3) | IrLowererSnapshotTests.TryCatch, .TryFinally; IrLowererTests.AThrowInsideATryGoesToTheCatchThatCatchesItsType, .TheFirstCatchThatTakesTheThrownTypeWins, .AFinallyRunsOnEveryExitPath, .ACallThatThrowsWhereSeveralCatchesCouldApplyIsOpaque, .WholeBodyOpaqueSpanIsTheConstructNotTheBody | M2-004, M3-025 |
| TypeOf | lowered: a closed type reads the shared `typeof.<T>` input, of `System.Type` sort, adding no trace event and never null; a method or type parameter is opaque with reason `TypeOf` | IrLowererTests.TypeOfIsAReadOfASharedInput, .TypeOfIsNeverNull, .UnsupportedConstructIsOpaqueWithItsName | P2-002 |
| Unary | lowered: `!`, `~`, `-` (checked: overflow edge), `+` on integral/bool; `-` on `float`, `double` and `decimal` is an `IrPure` of `f32.neg`, `f64.neg` or `dec.neg`, and `+` its operand (M4-002); a user-defined operator is an `IrPure` of `op:<identity>` (M4-002); a lifted operator, and anything else, opaque with reason `Unary` | IrLowererTests.CheckedNegationOverflowsOnlyAtMinValue, .BitwiseNotAndUnaryPlusLower, .UnsupportedConstructIsOpaqueWithItsName; PureLoweringTests.FloatingPointArithmeticNeverThrows, .UnaryPlusIsItsOperand, .UserDefinedOperatorsAndConversionsAreTheirOpFunctions, .LiftedOrWidenedOperatorsStayOpaque | M2-003, M4-002 |
| Using | lowered: as the CFG desugars it: the body in a `try` whose `finally` null-checks a reference resource (`IsNull`) and calls `IDisposable.Dispose()`, through the `cast.<From>.<To>` map when the CFG converts the resource | IrLowererSnapshotTests.UsingStatement | M2-004, M4-001 |
| UsingDeclaration | lowered: as `Using`, the `try` running to the end of the enclosing block | IrLowererSnapshotTests.UsingDeclaration | M2-004, M4-001 |
| VariableDeclaration | n/a: the CFG turns an initializer into a `SimpleAssignment` | IrLowererSnapshotTests.StraightLineArithmetic | M2-003 |
| VariableDeclarationGroup | n/a: as `VariableDeclaration` | IrLowererSnapshotTests.StraightLineArithmetic | M2-003 |
| VariableDeclarator | n/a: as `VariableDeclaration` | IrLowererSnapshotTests.StraightLineArithmetic | M2-003 |
| VariableInitializer | n/a: as `VariableDeclaration` | IrLowererSnapshotTests.StraightLineArithmetic | M2-003 |
