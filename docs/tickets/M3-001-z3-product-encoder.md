# M3-001 z3 product encoder
Status: todo
Effort: L
Depends on: M1-002

## Goal
IVerificationBackend over Microsoft.Z3: product program per section 5 with bitvectors, arrays, uninterpreted sorts and functions; model decoding into a readable counterexample. Soundness property harness per section 7 with an IR mutation generator (P vs P is Equivalent; P vs mutated P is never Equivalent).

## Spec references
ARCHITECTURE.md and VERIFICATION-MODEL.md; the implementer lists exact sections here before coding.

## Deliverables
- [ ] expand into concrete items before coding (see tickets/README.md template)
- [ ] tests: unit, plus the kinds named in the goal

## Out of scope
Anything not in the goal. Stub the next ticket's interface; do not implement it.

## Notes
