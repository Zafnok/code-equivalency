namespace Equiv.Cli;

internal static class Program
{
    // Router and argument parsing land in M1-005; skeleton exits cleanly with no args.
    public static int Main(string[] args)
    {
        return 0;
    }
}
