# Divergent
# Extra call: new also audits.
proc "T::M(int)" (%a: bv32) -> bv32 entry B0
B0:
  %r: bv32 = call "Svc::Get(int)"(%a)
  ret %r
---
proc "T::M(int)" (%a: bv32) -> bv32 entry B0
B0:
  %r: bv32 = call "Svc::Get(int)"(%a)
  call "Audit::Log()"()
  ret %r
